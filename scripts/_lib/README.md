# scripts/_lib

## What
Shared shell helpers used by script entrypoints.

## Philosophy
Keep reusable functions here so argument parsing, logging, and safety checks are implemented once and imported instead of duplicated.
