pub use crate::stages::semantics::{
    canonical_stage_order, fastq_stage_is_stable, optional_branches, stage_criticality, stage_kind,
    stage_metric_classes, stage_metric_invariants, stage_semantics, BoundaryInvariant,
    FastqStageKind, StageDefinition, StageSemantics, STAGE_BOUNDARY_INVARIANTS,
};
