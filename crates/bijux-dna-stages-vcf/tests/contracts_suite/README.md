# contracts_suite

This directory holds decomposed contract test helpers and focused test modules
used by `tests/contracts.rs`.

Taxonomy rationale:
- It is a structural submodule of the `contracts` bucket.
- Files here are not a new policy bucket; they support contract tests only.
