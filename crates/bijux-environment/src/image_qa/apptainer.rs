use anyhow::{anyhow, Result};

/// Scaffold for Apptainer image QA (mirrors Docker QA flow).
///
/// TODO: Implement with the same acceptance criteria as Docker QA.
#[allow(dead_code)]
pub(crate) fn run_apptainer_image_qa() -> Result<()> {
    Err(anyhow!("Apptainer image QA not implemented yet"))
}
