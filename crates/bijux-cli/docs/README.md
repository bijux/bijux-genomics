# bijux-cli

Command-line interface for bijux workflows.

Use this crate to interact with the bijux API for planning, executing, and inspecting runs.

See `ARCHITECTURE.md` for design boundaries and `COMMANDS.md` for user-facing commands.
