# CLI Commands

High-level groups:
- `fastq` pipeline commands (preprocess, validate, inspect)
- `bam` pipeline commands (align, qc, downstream)
- `bench` benchmarking commands
- `policies` audit commands

Use `bijux --help` for the authoritative list and flags.
