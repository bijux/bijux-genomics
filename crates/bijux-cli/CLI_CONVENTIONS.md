# CLI_CONVENTIONS

- Prefer explicit subcommands over flags.
- Validation belongs in `commands/validation.rs`.
- Formatting helpers belong in `commands/formatting.rs`.
- Rendering/report input helpers belong in `commands/rendering.rs`.
