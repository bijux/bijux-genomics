# Architecture

This document is intentionally brief. The canonical runtime contract lives in `RUNTIME_CONTRACT.md`.

## Pointers
- `RUNTIME_CONTRACT.md` for the authoritative contract narrative.
- `ARTIFACTS.md` for run-layout file inventory.
- `TESTS.md` for stability and integrity coverage.
