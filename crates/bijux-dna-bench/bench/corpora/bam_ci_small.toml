schema_version = "bijux.bench.corpus.v1"
corpus_id = "bam_ci_small"
domain = "bam"
scale = "ci-small"
scientific_caveats = [
  "ci-sized BAM fixtures are optimized for regression detection and policy boundaries, not for estimating biological rates",
]

[[datasets]]
dataset_id = "bam_valid_coordinate_sorted"
fixture = "fixtures/bam/valid_coordinate_sorted"
read_layout = "paired"
class_label = "valid"
case_tags = ["valid", "coordinate-sorted"]
[datasets.truth_set]
status = "available"
truth_set_id = "truth.bam.synthetic.valid-coordinate.v1"
note = "synthetic alignment truth supports deterministic QC expectations"

[[datasets]]
dataset_id = "bam_missing_index"
fixture = "fixtures/bam/missing_index"
read_layout = "paired"
class_label = "missing_index"
case_tags = ["missing-index", "boundary"]
[datasets.truth_set]
status = "unavailable"
note = "no truth set; missing index case asserts contract refusal semantics"

[[datasets]]
dataset_id = "bam_duplicate_heavy"
fixture = "fixtures/bam/duplicate_heavy"
read_layout = "paired"
class_label = "duplicate_heavy"
case_tags = ["duplicate-heavy", "markdup"]
[datasets.truth_set]
status = "available"
truth_set_id = "truth.bam.synthetic.duplicate-heavy.v1"
note = "synthetic duplicate labels provide controlled duplicate burden truth"

[[datasets]]
dataset_id = "bam_low_coverage"
fixture = "fixtures/bam/low_coverage"
read_layout = "paired"
class_label = "low_coverage"
case_tags = ["low-coverage", "sparse"]
[datasets.truth_set]
status = "unavailable"
note = "no truth set; low-coverage class is used to verify warning calibration only"

[[datasets]]
dataset_id = "bam_damage_like"
fixture = "fixtures/bam/damage_like"
read_layout = "paired"
class_label = "damage_like"
case_tags = ["damage-like", "ancient-dna"]
[datasets.truth_set]
status = "unavailable"
note = "no truth set; damage-like pattern is heuristic and must remain caveated"

[[datasets]]
dataset_id = "bam_reference_mismatch"
fixture = "fixtures/bam/reference_mismatch"
read_layout = "paired"
class_label = "reference_mismatch"
case_tags = ["reference-mismatch", "contig-alias"]
[datasets.truth_set]
status = "unavailable"
note = "no truth set; mismatch fixture validates reference compatibility diagnostics"

[[backend_comparisons]]
comparison_id = "bam.align.ci"
stage_id = "bam.align"
tools = ["bwa", "bowtie2"]
caveat = "aligner comparison is scenario-specific and cannot be interpreted as universal mapping truth"

[[drift_scenarios]]
scenario_id = "bam.backend.version.ci"
drift_axis = "backend-version"
baseline_label = "samtools-1.18"
candidate_label = "samtools-1.20"
caveat = "backend-version drift reflects toolchain change sensitivity and not universal scientific quality"
