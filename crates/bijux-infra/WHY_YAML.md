# WHY_YAML

YAML support is retained only for legacy configuration files that have not yet
migrated to TOML/JSON. The YAML API surface is intentionally narrow and gated
behind the `yaml` feature flag.
