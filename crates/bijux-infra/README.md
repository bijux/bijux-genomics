# bijux-infra

## What this crate owns (SSOT)
- Pure utilities with no domain semantics.

## What this crate must never do (purity)
- Execution, domain catalogs, or selection.

## What depends on this crate
- Many crates for helpers.

## What this crate depends on
- Minimal common deps.
