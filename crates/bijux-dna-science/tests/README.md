# bijux-dna-science Tests

Science tests follow the workspace taxonomy even when the crate keeps lightweight integration
tests at the tests root. Bucket directories document the governed intent surfaces for policy
discovery.
