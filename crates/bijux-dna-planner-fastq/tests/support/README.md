# Planner Support Fixtures

Purpose: shared test-only helpers for planner-fastq contract and snapshot coverage.

- `tool_registry.rs` loads governed domain tool metadata for planner integration tests without depending on runtime wiring.
- Keep this directory limited to reusable planner-test support code and lightweight documentation.
