owner = "bijux-dna-analyze"
responsibility = "metric semantics lookup and missing-data policy"
may_depend_on = ["runtime metric semantics"]
must_not_depend_on = ["load", "report", "pipeline", "filesystem io"]
