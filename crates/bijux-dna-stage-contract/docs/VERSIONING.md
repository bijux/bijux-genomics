# VERSIONING

Breaking changes require a major bump. Examples:
- Removing a field
- Renaming a field
- Changing semantics of an existing field

Non-breaking changes (minor bump):
- Adding an optional field
- Adding a new enum variant with safe default handling
