# Planner Plan Inputs (Default)

Minimal placeholder inputs for plan snapshot tests.

Purpose: provide stable, on-repo file paths for BAM + FASTQ + reference assets
so planner tests do not depend on temp directories or external files.

Files are empty; tests assert structure, not content.
