# PUBLIC_API

This crate must stay tiny and deliberate.

## Exported surface
- `snapshots::stable_json` — canonical JSON key ordering for tests.

Anything else must be added intentionally with policy + documentation updates.
