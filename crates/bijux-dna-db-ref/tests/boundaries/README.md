# Boundaries

Boundary tests in this crate enforce:

- the documented source tree,
- stable public-surface layout,
- ownership boundaries between runtime config, models, providers, and lookup behavior.
