//! Pipeline registry accessors.

pub use bijux_dna_pipelines::registry::PipelineRegistry;
pub use bijux_dna_pipelines::{Domain, PipelineProfile};

pub use crate::run::{select_pipeline, select_pipelines};
