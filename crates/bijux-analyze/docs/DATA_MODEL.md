# DATA_MODEL

Primary inputs:
- run artifacts (manifests, metrics envelopes, summaries)
- run index rows and facts

Primary outputs:
- report bundles (json/tsv/html)
- decision traces and comparisons

Schema registry lives under `aggregate/schema/` and is the single source of metric definitions.
