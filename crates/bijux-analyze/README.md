# bijux-analyze

## What this crate owns (SSOT)
- Report generation from run artifacts.

## What this crate must never do (purity)
- Execute tools or select them.

## What depends on this crate
- API, benchmark.

## What this crate depends on
- core, runtime, domain crates.
