# SSOT NOTICE: This file is generated by bijux-std standards sync.
# Source of truth: bijux-std/.github/standards/repo-config.manifest.json and bijux-std/shared/bijux-gh/workflows/.
# Do not edit this generated copy directly.

name: "continuous integration"
on:
  pull_request:
    branches:
      - main
  merge_group:
    branches:
      - main
  workflow_dispatch:
concurrency:
  group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}"
  cancel-in-progress: true
permissions:
  contents: read
env:
  RUST_TOOLCHAIN_VERSION: 1.95.0
  CARGO_DENY_VERSION: 0.18.3
  CARGO_AUDIT_VERSION: 0.22.0
  CARGO_NEXTEST_VERSION: 0.9.100
  SCCACHE_GHA_ENABLED: true
  RUSTC_WRAPPER: sccache
jobs:
  fmt:
    name: format
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
          components: rustfmt
      - name: "Set up sccache"
        uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-fmt
      - name: "Run repository formatting gate"
        run: "make fmt"
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  lint:
    name: lint
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: "Install Linux build dependencies"
        run: "sudo apt-get update && sudo apt-get install --yes pkg-config libfontconfig1-dev libfreetype6-dev"
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
          components: clippy
      - name: "Set up sccache"
        uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-lint
      - name: "Run repository lint gate"
        run: "make lint"
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  audit:
    name: "dependency audit"
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - name: "Set up sccache"
        uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-audit
      - name: "Install Rust audit tools"
        run: "cargo install --locked cargo-deny --version \"${{ env.CARGO_DENY_VERSION }}\" && cargo install --locked cargo-audit --version \"${{ env.CARGO_AUDIT_VERSION }}\""
      - name: "Run repository audit gate"
        run: "make audit"
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  test:
    name: test
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: "Check out the repository"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: "Install Linux build dependencies"
        run: "sudo apt-get update && sudo apt-get install --yes pkg-config libfontconfig1-dev libfreetype6-dev"
      - name: "Set up Rust"
        uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - name: "Set up sccache"
        uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - name: "Restore the Rust cache"
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-test
      - name: "Install cargo-nextest"
        run: "cargo install --locked cargo-nextest --version \"${{ env.CARGO_NEXTEST_VERSION }}\""
      - name: "Run repository test gate"
        run: "make test"
    if: "${{ github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]' }}"
  ssot_policy:
    name: ssot-policy-fast
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "SSOT policy fast gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make ssot-policy-fast
          
  workspace_hygiene:
    name: workspace-hygiene
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Workspace formatting and lint gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make fmt
            make lint-workspace
          
  unit_contract_fast:
    name: unit-contract-fast
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Unit and contract gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make _unit-contract-fast
          
  release_readiness:
    name: release-readiness
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Release readiness gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make _release-readiness
          
  fastq_container_readiness:
    name: fastq-container-readiness
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "FASTQ container readiness gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make _fastq-container-readiness
          
  profile_invariants:
    name: profile-invariants
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Profile invariants gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make _test-profile-invariants
          
  cargo_policy:
    name: cargo-policy
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Cargo policy gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make _policy-no-raw-cargo
          
  fast-tier:
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (always() && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]')))) }}"
    runs-on: ubuntu-latest
    needs:
      - fmt
      - lint
      - audit
      - test
      - ssot_policy
      - workspace_hygiene
      - unit_contract_fast
      - release_readiness
      - fastq_container_readiness
      - profile_invariants
      - cargo_policy
    steps:
      - name: "Require fast CI lanes to pass"
        shell: bash
        env:
          FMT_RESULT: "${{ needs.fmt.result }}"
          LINT_RESULT: "${{ needs.lint.result }}"
          AUDIT_RESULT: "${{ needs.audit.result }}"
          TEST_RESULT: "${{ needs.test.result }}"
          SSOT_POLICY_RESULT: "${{ needs.ssot_policy.result }}"
          WORKSPACE_HYGIENE_RESULT: "${{ needs.workspace_hygiene.result }}"
          UNIT_CONTRACT_FAST_RESULT: "${{ needs.unit_contract_fast.result }}"
          RELEASE_READINESS_RESULT: "${{ needs.release_readiness.result }}"
          FASTQ_CONTAINER_READINESS_RESULT: "${{ needs.fastq_container_readiness.result }}"
          PROFILE_INVARIANTS_RESULT: "${{ needs.profile_invariants.result }}"
          CARGO_POLICY_RESULT: "${{ needs.cargo_policy.result }}"
        run:
          |
            set -euo pipefail
            for result in \
              "${FMT_RESULT}" \
              "${LINT_RESULT}" \
              "${AUDIT_RESULT}" \
              "${TEST_RESULT}" \
              "${SSOT_POLICY_RESULT}" \
              "${WORKSPACE_HYGIENE_RESULT}" \
              "${UNIT_CONTRACT_FAST_RESULT}" \
              "${RELEASE_READINESS_RESULT}" \
              "${FASTQ_CONTAINER_READINESS_RESULT}" \
              "${PROFILE_INVARIANTS_RESULT}" \
              "${CARGO_POLICY_RESULT}"; do
              [[ "${result}" == "success" ]]
            done
          
  benchmark-active-scope:
    runs-on: ubuntu-latest
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Active benchmark readiness"
        run:
          |
            make bench-active-fast
          
  parser-fixtures:
    runs-on: ubuntu-latest
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Parser fixtures"
        run:
          |
            make bench-parser-fast
          
  adapter-rendering:
    runs-on: ubuntu-latest
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Adapter rendering"
        run:
          |
            make bench-adapter-fast
          
  science-fixtures:
    runs-on: ubuntu-latest
    if: "${{ (github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]') && (github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group' || (github.event_name == 'pull_request' && github.event.pull_request.user.type != 'Bot' && !endsWith(github.event.pull_request.user.login, '[bot]'))) }}"
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: "${{ env.RUST_TOOLCHAIN_VERSION }}"
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Science fixtures"
        run:
          |
            make science-fixtures-fast
          
