# SSOT NOTICE: This file is generated by bijux-std standards sync.
# Source of truth: bijux-std/.github/standards/repo-config.manifest.json and bijux-std/shared/bijux-gh/workflows/.
# Do not edit this generated copy directly.

name: ci
on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      run_slow_tier:
        description: "Run slow/manual CI tier"
        required: false
        type: boolean
        default: false
jobs:
  fast-tier:
    runs-on: ubuntu-latest
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.88.0
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Minimal CI gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make ssot-policy-fast
            make ci-fast
          
  slow-tier:
    if: "${{ github.event_name == 'workflow_dispatch' && inputs.run_slow_tier == true }}"
    runs-on: ubuntu-latest
    needs: fast-tier
    env:
      SCCACHE_GHA_ENABLED: true
      RUSTC_WRAPPER: sccache
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.88.0
      - uses: mozilla/sccache-action@7d986dd989559c6ecdb630a3fd2557667be217ad
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Slow CI gate"
        env:
          TZ: UTC
          LC_ALL: C
        run:
          |
            make ci-slow
          
  example-dry-run:
    runs-on: ubuntu-latest
    needs: fast-tier
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.88.0
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Example schema + dry plan"
        run:
          |
            make ci-fast
          
  registry-vs-containers:
    runs-on: ubuntu-latest
    needs: fast-tier
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.88.0
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Registry tools must have container defs"
        run:
          |
            make ci-fast
          
  container-orphans:
    runs-on: ubuntu-latest
    needs: fast-tier
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9
        with:
          toolchain: 1.88.0
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4
        with:
          shared-key: ci-build
      - name: "Container defs must exist in registry"
        run:
          |
            make ci-fast
          
