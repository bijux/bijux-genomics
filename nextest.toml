[profile.default]
retries = { count = 1, backoff = "fixed", delay = "1s" }

[profile.ci]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = false

[profile.slow]
retries = { count = 1, backoff = "fixed", delay = "1s" }

[profile.e2e]
retries = { count = 0, backoff = "fixed", delay = "1s" }

[profile.science]
retries = { count = 1, backoff = "fixed", delay = "1s" }
