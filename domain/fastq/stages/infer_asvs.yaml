extends: "_schema.yaml"
stage_id: "fastq.infer_asvs"
status: "planned"
scope: "pre_hpc_pre_vcf"
domain: "fastq"
inputs:
  - name: "reads"
    data_type: "fastq"
    cardinality: "Many"
outputs:
  - name: "asv_table_tsv"
    data_type: "tsv"
    cardinality: "One"
  - name: "asv_sequences_fasta"
    data_type: "fasta"
    cardinality: "One"
  - name: "taxonomy_ready_fasta"
    data_type: "fasta"
    cardinality: "One"
  - name: "taxonomy_ready_fastq"
    data_type: "fastq"
    cardinality: "One"
parameters:
  - name: "denoising_method"
    param_type: "string"
    default: "dada2"
metrics:
  - name: "asv_count"
    meaning: "Number of inferred ASVs"
  - name: "sample_count"
    meaning: "Number of samples represented in the ASV table"
description: "FASTQ stage for amplicon denoising and ASV table generation."
mutates_fastq: false
report_only: false
may_change_read_count: false
image_requirements:
  needs:
    - gzip
    - r
  forbids:
    - network
required_inputs:
  - "reads"
required_outputs:
  - "asv_table_tsv"
  - "asv_sequences_fasta"
  - "taxonomy_ready_fasta"
  - "taxonomy_ready_fastq"
tool_capability_requirements: []
invariants:
  - "stable_artifact_contract"
  - "amplicon_only_stage"
  - "representative_sequences_match_feature_table"
allowed_missingness:
  - "none"
compatible_tools:
  - "dada2"
defaults_source: "doc_ref:domain/fastq/docs/DEFAULT_SETTINGS.md"
assumptions:
  - "asv inference applies only to amplicon libraries with denoising-aware governance."
bank_hooks:
  - "none"
metrics_schema: "bijux.stage.metrics.v1"
planned_out_of_scope:
  - "dada2"
