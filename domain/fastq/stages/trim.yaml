extends: "_schema.yaml"
stage_id: "fastq.trim"
inputs:
  - name: "reads"
    data_type: "fastq"
    cardinality: "Many"
outputs:
  - name: "trimmed_reads"
    data_type: "fastq"
    cardinality: "Many"
parameters:
  - name: "min_length"
    param_type: "int"
    default: "30"
metrics:
  - name: "reads_in"
    meaning: "Number of input reads"
  - name: "reads_out"
    meaning: "Number of output reads"
  - name: "bases_in"
    meaning: "Number of input bases"
  - name: "bases_out"
    meaning: "Number of output bases"
  - name: "mean_q_before"
    meaning: "Mean base quality before trimming"
  - name: "mean_q_after"
    meaning: "Mean base quality after trimming"
description: "FASTQ stage for trimming and preprocessing reads."
