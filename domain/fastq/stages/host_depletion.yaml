extends: "_schema.yaml"
stage_id: "fastq.host_depletion"
status: "supported"
scope: "pre_hpc_pre_vcf"
domain: "fastq"
inputs:
  - name: "reads"
    data_type: "fastq"
    cardinality: "Many"
  - name: "reference_index"
    data_type: "ref_index"
    cardinality: "One"
outputs:
  - name: "filtered_fastq"
    data_type: "fastq"
    cardinality: "Many"
  - name: "screen_report"
    data_type: "json"
    cardinality: "One"
parameters:
  - name: "host_identity_threshold"
    param_type: "float"
    default: "0.95"
  - name: "retain_unmapped_only"
    param_type: "bool"
    default: "true"
metrics:
  - name: "reads_in"
    meaning: "Number of input reads."
  - name: "reads_out"
    meaning: "Number of reads after host depletion."
  - name: "contamination_rate"
    meaning: "Estimated host-mapped read fraction."
description: "FASTQ stage for host read depletion via reference mapping."
mutates_fastq: true
report_only: false
may_change_read_count: true
image_requirements:
  needs:
    - gzip
    - reference_index
  forbids:
    - network
required_inputs:
  - "reads"
  - "reference_index"
required_outputs:
  - "filtered_fastq"
  - "screen_report"
tool_capability_requirements:
  - "bam_input"
invariants:
  - "stable_artifact_contract"
  - "deterministic_output_order"
allowed_missingness:
  - "none"
compatible_tools:
  - bowtie2
  - samtools
assumptions:
  - "reference index must match host depletion reference and naming conventions."
bank_hooks:
  - "reference_bank"
metrics_schema: "bijux.stage.metrics.v1"
planned_out_of_scope: []
