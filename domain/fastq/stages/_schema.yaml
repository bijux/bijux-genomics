schema_version: "bijux.stage.v3"
required_fields:
  - stage_id
  - status
  - scope
  - domain
  - inputs
  - outputs
  - description
  - invariants
  - compatible_tools
  - assumptions
  - defaults_source
  - bank_hooks
  - metrics_schema
  - tool_capability_requirements
  - required_inputs
  - required_outputs
  - allowed_missingness
allowed_status:
  - supported
  - planned
  - out_of_scope
required_scope: "pre_hpc_pre_vcf"
domain: "fastq"
