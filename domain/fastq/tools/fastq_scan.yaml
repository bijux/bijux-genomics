schema_version: bijux.tool.v2
tool_id: fastq_scan
upstream: https://github.com/indraniel/fastq_scan
versioning_strategy: pinned
pin_strategy: pinned
default_version: planned
install_kind: container
license: GPL-3.0
citation: pending:tool-publication
planned_stage_ids:
- fastq.validate_reads
- fastq.profile_overrepresented_sequences
stage_contracts:
  fastq.validate_reads:
    required_inputs:
    - reads_r1
    expected_artifacts:
    - validation_report
    notes: Validation remains report-only and would emit the governed validation report artifact.
  fastq.profile_overrepresented_sequences:
    required_inputs:
    - reads_r1
    expected_artifacts:
    - overrepresented_sequences_tsv
    - overrepresented_sequences_json
    notes: Overrepresented-sequence profiling remains report-only and would emit governed ranked summaries.
status: planned
scope: pre_hpc_pre_vcf
expected_artifacts:
- validation_report
- overrepresented_sequences_json
- overrepresented_sequences_tsv
metrics_schema_id: bijux.fastq.qc.v1
metrics_schema: bijux.fastq.qc.v1
version_cmd: fastq_scan --version
help_cmd: fastq_scan --help
comparability:
  comparable_with:
  - fastqvalidator
  - fastqc
  non_comparable_with: []
comparability_notes: Comparable only within same stage contract and input constraints.
notes: Domain SSOT tool contract entry.
capabilities:
- SE
- PE
- qc
