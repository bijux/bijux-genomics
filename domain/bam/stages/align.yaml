extends: "_schema.yaml"
stage_id: "bam.align"
inputs:
  - name: "bam"
    data_type: "bam"
    cardinality: "One"
outputs:
  - name: "align_report_json"
    data_type: "json"
    cardinality: "One"
parameters: []
metrics:
  - name: "runtime_s"
    meaning: "Wall-clock runtime in seconds"
  - name: "memory_mb"
    meaning: "Peak memory usage in MB"
description: "BAM align stage contract."
required_inputs:
  - bam
required_outputs:
  - align_report_json
tool_capability_requirements:
  - bam_input
invariants:
  - stable_artifact_contract
  - deterministic_output_order
planned_out_of_scope: []
