schema_version: "bijux.stage.v1"
stage_id: "bam.validate"
status: "supported"
scope: "pre_hpc_pre_vcf"
domain: "bam"
inputs:
  - name: "bam"
    data_type: "bam"
    cardinality: "One"
outputs:
  - name: "report_json"
    data_type: "json"
    cardinality: "One"
parameters: []
metrics:
  - name: "runtime_s"
    meaning: "Wall-clock runtime in seconds"
  - name: "memory_mb"
    meaning: "Peak memory usage in MB"
description: "Stub BAM validation stage."
required_inputs: []
required_outputs: []
tool_capability_requirements: []
invariants:
  - "stable_artifact_contract"
  - "deterministic_output_order"
allowed_missingness:
  - "none"
  - "stable_artifact_contract"
  - "deterministic_output_order"
compatible_tools:
  - samtools
  - bedtools
  - bamtools
assumptions:
  - "default stage assumptions captured"
metrics_schema: "bijux.stage.metrics.v1"
planned_out_of_scope: []
