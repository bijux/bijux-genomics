schema_version: "bijux.stage.v1"
stage_id: "vcf.imputation_metrics"
status: "supported"
scope: "post_vcf"
domain: "vcf"
description: "VCF stage contract for vcf.imputation_metrics in the governed domain registry."
inputs:
  - name: "vcf"
    data_type: "vcf"
    cardinality: "One"
outputs:
  - name: "imputation_out"
    data_type: "json"
    cardinality: "One"
required_inputs: ["vcf"]
required_outputs: ["imputation_out"]
tool_capability_requirements: []
metrics:
  - name: "imputation_status"
compatible_tools: ["beagle", "glimpse", "impute5", "minimac4"]
defaults_source: "doc_ref:domain/vcf/docs/DEFAULT_SETTINGS.md"
assumptions: ["panel_required", "genetic_map_required"]
bank_hooks: ["none"]
invariants: ["stable_artifact_contract"]
allowed_missingness:
  - "none"
planned_out_of_scope: []
