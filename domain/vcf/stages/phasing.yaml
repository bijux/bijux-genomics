schema_version: "bijux.stage.v1"
stage_id: "vcf.phasing"
status: "supported"
scope: "post_vcf"
domain: "vcf"
description: "VCF stage contract for vcf.phasing in the governed domain registry."
inputs:
  - name: "vcf"
    data_type: "vcf"
    cardinality: "One"
outputs:
  - name: "phased_vcf"
    data_type: "vcf"
    cardinality: "One"
required_inputs: ["vcf"]
required_outputs: ["phased_vcf"]
tool_capability_requirements: []
metrics:
  - name: "phasing_status"
compatible_tools: ["beagle", "shapeit5", "eagle"]
defaults_source: "doc_ref:domain/vcf/docs/DEFAULT_SETTINGS.md"
assumptions: ["panel_required", "genetic_map_required"]
bank_hooks: ["none"]
invariants: ["stable_artifact_contract"]
allowed_missingness:
  - "none"
planned_out_of_scope: []
