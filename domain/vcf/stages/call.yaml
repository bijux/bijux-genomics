schema_version: "bijux.stage.v1"
stage_id: "vcf.call"
status: "supported"
scope: "post_vcf"
domain: "vcf"
description: "VCF stage contract for vcf.call in the governed domain registry."
inputs:
  - name: "vcf"
    data_type: "vcf"
    cardinality: "One"
outputs:
  - name: "called_vcf"
    data_type: "vcf"
    cardinality: "One"
required_inputs: ["vcf"]
required_outputs: ["called_vcf"]
tool_capability_requirements: []
metrics:
  - name: "called_variants"
compatible_tools: ["bcftools"]
single_tool_justification: "bcftools is the only production-approved caller in current VCF baseline."
defaults_source: "doc_ref:domain/vcf/docs/DEFAULT_SETTINGS.md"
assumptions: ["diploid_calling"]
bank_hooks: ["none"]
invariants: ["tool_pinned"]
allowed_missingness:
  - "none"
planned_out_of_scope: []
