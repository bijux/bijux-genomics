# Disposable-Root Cleanup Proof

This directory stores tracked proof that benchmark truth survives deleting disposable repository
roots.

`DELETE_DISPOSABLE_ROOTS_SAFE.json` is regenerated by
`bijux-dna bench paths prove-disposable-root-cleanup` after removing `target/`, `artifacts/`, and
`runs/`, then rerunning strict benchmark path validation.
