# schema_version = 1
# owner = bijux-dna-planner-fastq
# purpose = Governed local-ready plan input for fastq.deplete_host.
# authority = bijux-dna-planner-fastq
# stability = evolving
# last_updated = 2026-06-01

schema_version = "bijux.bench.fastq.local_deplete_host.v1"
input_r1 = "assets/toy/core-v1/fastq/reads_1.fastq"
reference_index = "assets/reference/host/references/toy_host_reference"
tool_id = "bowtie2"
threads = 4
output_dir = "target/local-ready/fastq.deplete_host"
