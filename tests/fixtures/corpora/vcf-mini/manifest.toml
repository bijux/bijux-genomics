schema_version = "bijux.bench.vcf_corpus_fixture.v1"
corpus_id = "vcf-mini"
reference_id = "vcf-mini-reference"
description = "Tiny governed VCF corpus with single-sample, cohort, phased, and reference-panel variant views plus sample and population metadata for local contract checks."
reference_fasta_path = "reference/vcf_mini_reference.fasta"
reference_fasta_index_path = "reference/vcf_mini_reference.fasta.fai"
raw_vcf_path = "variants/vcf_mini_raw_single_sample.vcf"
filtered_vcf_path = "variants/vcf_mini_filtered_single_sample.vcf"
multisample_vcf_path = "variants/vcf_mini_multisample.vcf"
phased_vcf_path = "variants/vcf_mini_phased.vcf"
panel_vcf_path = "variants/vcf_mini_reference_panel.vcf"
target_sites_bed_path = "regions/vcf_mini_target_sites.bed"
sample_metadata_path = "metadata/sample_metadata.tsv"
population_metadata_path = "metadata/population_metadata.tsv"
expected_raw_sample_ids = ["sample_a"]
expected_filtered_sample_ids = ["sample_a"]
expected_multisample_sample_ids = ["sample_a", "sample_b", "sample_c", "sample_d"]
expected_phased_sample_ids = ["sample_a", "sample_b", "sample_c", "sample_d"]
expected_panel_sample_ids = ["panel_ref_1", "panel_ref_2"]

