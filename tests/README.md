# Root Tests

Benchmark fixture content is owned by `benchmarks/tests/fixtures/`.

The root `tests/fixtures` path is a compatibility symlink that preserves stable
repository-relative fixture paths for callers that still resolve
`tests/fixtures/...`.
