# Report Contract

`report.json` must include:
- pipeline and profile identifiers
- tool versions + image digests
- parameters + defaults ledger
- input hashes + run hash
- stage contract hashes
- metric summaries with units and provenance

A report is complete when all required sections for the pipeline are present and all required metrics are populated.
