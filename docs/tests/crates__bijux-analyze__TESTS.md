# Test Ownership

- unit: metric schema validation, versioning, invariants
- integration: sqlite persistence and record serialization
- regression: benchmark report consistency (gated by higher-level suites)
