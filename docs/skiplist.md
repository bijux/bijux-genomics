# Skiplist

Tools temporarily excluded from build/test gates.
This file is intentionally explicit so failures are visible and tracked.

## Images excluded
- seqpurge (ngs-bits build fails on docker-mac-arm64)
