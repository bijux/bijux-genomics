# schema_version = 1
# owner = bijux-dna-policies
# purpose = Contract config for configs/ci/registry/deprecations.toml
# authority = bijux-dna-policies
# stability = stable
# last_updated = 2026-02-13

[[deprecations]]
tool_id = "bamtools"
stage = "bam.validate"
deprecated_since = "2026-01-01"
removal_after = "2026-07-01"
rationale = "Example policy entry used to enforce deprecation-window contract checks."
replacement = "samtools"

[[deprecations]]
stage = "fastq.asv_inference"
deprecated_since = "2026-02-15"
removal_after = "2026-12-31"
rationale = "Out-of-scope by policy: use-case is documented as presentation/example-only until certified support exists."
replacement = "fastq.otu_clustering"
