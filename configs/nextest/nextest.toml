# schema_version = 1
# owner = bijux-dna-infra
# purpose = Contract config for configs/nextest/nextest.toml
# authority = bijux-dna-infra
# stability = stable
# last_updated = 2026-02-13

[profile.default]
retries = { count = 1, backoff = "fixed", delay = "1s" }

[profile.ci]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = false
test-threads = 1
failure-output = "immediate"
success-output = "never"
status-level = "pass"

[profile.slow]
retries = { count = 1, backoff = "fixed", delay = "1s" }

[profile.e2e]
retries = { count = 0, backoff = "fixed", delay = "1s" }

[profile.science]
retries = { count = 1, backoff = "fixed", delay = "1s" }

[profile.flake]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = true
test-threads = 1
failure-output = "immediate"
success-output = "never"
status-level = "fail"
