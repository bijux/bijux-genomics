# configs/nextest

## What
Configuration files for the nextest domain.

## Philosophy
Keep nextest configuration scoped to this directory so ownership is explicit and drift is easy to detect.

## Files
- `configs/nextest/nextest.toml`
