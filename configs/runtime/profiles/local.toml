# schema_version = 1
# owner = bijux-dna-infra
# purpose = Contract config for configs/runtime/profiles/local.toml
# authority = bijux-dna-infra
# stability = stable
# last_updated = 2026-02-13

container_runtime = "docker"
default_threads = 4
default_mem_gb = 8
default_time_minutes = 60
run_base_dir = "./runs"
image_pull_policy = "if_missing"
use_case = "default local development profile"
