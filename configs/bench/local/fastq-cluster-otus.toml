# schema_version = 1
# owner = bijux-dna-planner-fastq
# purpose = Governed local benchmark fixture plan for fastq.cluster_otus.
# authority = bijux-dna-planner-fastq
# stability = evolving
# last_updated = 2026-06-22

schema_version = "bijux.bench.fastq.local_cluster_otus.v1"
tool_id = "vsearch"
threads = 1
otu_identity = 0.97
output_dir = "runs/bench/local-smoke/fastq.cluster_otus"

[[cases]]
sample_id = "corpus-03-otu-cluster-se"
reads = "assets/toy/corpus-03-amplicon-mini/fastq/merged_amplicon_reads.fastq"
