# schema_version = 1
# owner = bijux-dna-infra
# purpose = Clippy threshold policy for repository-wide Rust linting
# authority = bijux-dna-infra
# stability = stable
# last_updated = 2026-03-24

too-many-lines-threshold = 900
too-many-arguments-threshold = 14
max-struct-bools = 20
enum-variant-size-threshold = 1024
