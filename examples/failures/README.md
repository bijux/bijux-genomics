# Refusal Bundles

These directories capture governed negative-example bundles for Level 1 refusal classes.

Contracts:
- They are documentation and machine-readable refusal fixtures, not runnable example ids.
- They must not appear in `examples/index.yaml`.
- Each bundle must carry `README.md` and `refusal-bundle.json`.
