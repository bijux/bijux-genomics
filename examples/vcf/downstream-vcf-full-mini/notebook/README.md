# Optional notebook

This optional notebook is for presentation-only plotting and quick artifact inspection.

Outputs remain reproducible from CLI via `./scripts/run.sh examples run vcf_downstream_vcf_full_mini`.

Notebook execution is non-benchmarking and must not be required for correctness contracts.
