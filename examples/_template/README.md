# example-XYZ

## Purpose
Describe why this example exists and what contract it validates.

## Stage
- Stage ID: `<domain.stage_id>`
- Domain family: `<fastq|bam|vcf>`

## Inputs
List source accession/project, selection policy, and expected corpus scope.

## Outputs
List generated artifacts: manifests, telemetry, reports, and golden outputs.

## Acceptance Criteria
List deterministic checks required for this example to be considered valid.

## Golden Refresh
- Regenerate goldens and corpus checksums via:
  - `examples/_template/make.sh <example-id>`

## HPC Run
Document the exact `bijux` commands to run this example on HPC.
